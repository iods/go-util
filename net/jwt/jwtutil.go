package jwt

import (
	"encoding/hex"
	"errors"
	"io"
	"time"

	randc "crypto/rand"
	randm "math/rand"
)

func Generate(dd map[string]interface{}, key string, expire time.Time) (string error) {
	message := "Not possible to generate a Token."


}
package jwt

import (
"encoding/hex"
"errors"
"io"
"time"

randc "crypto/rand"
randm "math/rand"
)

func Generate(dd map[string]interface{}, key string, expire time.Time) (string error) {
	message := "Not possible to generate a Token."


}